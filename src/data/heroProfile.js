/**
 * Swap `imageSrc` / `imageAlt` when you replace the portrait asset.
 * Sizing is handled in CSS (`.hero__frame` / `.hero__img`) — no fixed pixel layout in JS.
 */
export const heroProfile = {
  imageSrc: '/assets/hero-img.webp',
  imageAlt: 'Portrait of Richie Richardo',
}
